general:
  branches:
    ignore:
      - master
machine:
  timezone: Asia/Tokyo
  pre:
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.23.3/install.sh | bash
  node:
    version: 0.12
deployment:
  master:
    branch: src
    commands:
      - sh script/deploy.sh
